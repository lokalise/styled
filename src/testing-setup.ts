import "jest-styled-components";
// This just forces types to be preserved in .d.ts so proper typings
// are added to global jest matcher.
export { StyledComponentsSerializerOptions } from "jest-styled-components";
